import React from 'react';
import PropTypes from 'prop-types';
import './Banner.css';

const Banner = () => (
  <div className="Banner">
    Banner Component
  </div>
);

Banner.propTypes = {};

Banner.defaultProps = {};

export default Banner;
