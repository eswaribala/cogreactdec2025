import React from 'react';
import PropTypes from 'prop-types';
import './Title.css';

const Title = () => (
  <div className="Title">
    Title Component
  </div>
);

Title.propTypes = {};

Title.defaultProps = {};

export default Title;
